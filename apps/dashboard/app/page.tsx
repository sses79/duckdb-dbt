// Placeholder until the dashboard page is built (slice 4). The page reads the tenant's publication
// through src/dashboard/reader.ts on the server only; nothing here runs in the browser.
export default function Page() {
  return (
    <main>
      <h1>School Wellbeing Signals</h1>
      <p>The dashboard is being built.</p>
    </main>
  );
}
